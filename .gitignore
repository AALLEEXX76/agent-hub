.venv/
__pycache__/
*.pyc
artifacts/
.env
.agent_env

# backups
*.bak
*.bak.*
*_bak*
*.bak_*

# local server snapshots (do not commit)

workflows/

# local backups
local_backups/

# local workflow drafts
workflow_agent_executor*.put.json

# keep snapshots index tracked (archives stay local)

# keep snapshots index tracked (archives stay local)
snapshots/*.tgz
snapshots/*.tgz.sha256
snapshots/*.sha256

# keep snapshots index tracked (archives stay local)
snapshots/*
!snapshots/INDEX.txt

# local junk / secrets artifacts
except
try:
key=env[N8N_API_KEY]
artifacts/
# snapshot checksum files generated locally
snapshots/*.sha256
snapshots/*.tgz.sha256
# --- local-ignore (managed) ---
# local junk / secrets artifacts
except
try:
key=env[N8N_API_KEY]
artifacts/
# keep snapshot tgz local, allow tracking checksums/index
snapshots/*.tgz
!snapshots/INDEX.txt
# --- /local-ignore ---
